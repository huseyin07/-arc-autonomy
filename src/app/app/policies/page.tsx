import { Ban, CirclePause, Gauge, ShieldCheck, UserCheck, WalletCards, Zap } from "lucide-react";
import { demoAgents, demoIntents } from "@/data/demo";
import { EvaluationCard } from "@/features/policies/evaluation-card";
import { PageHeader, Status } from "@/components/ui";
const primitives=[{n:"Daily spending limit",i:Gauge},{n:"Per-transaction limit",i:WalletCards},{n:"Approval threshold",i:UserCheck},{n:"Allowed recipient",i:ShieldCheck},{n:"Blocked recipient",i:Ban},{n:"Allowed asset",i:Zap},{n:"Emergency stop",i:CirclePause}];
export default function PoliciesPage(){return <><PageHeader eyebrow="Deterministic guardrails" title="Policy engine" action={<Status tone="ok">Engine active</Status>}/><p className="mb-8 max-w-2xl text-sm leading-6 text-muted">Auditable primitives separate authorization decisions from future execution. Evaluations below are generated by the real Phase 1 policy function against isolated demo data.</p><div className="grid gap-px border border-line bg-line sm:grid-cols-2 xl:grid-cols-4">{primitives.map(({n,i:Icon})=><div key={n} className="flex items-center gap-3 bg-panel p-4 text-sm"><Icon size={16} className="text-acid"/>{n}</div>)}</div><div className="mt-10"><div className="eyebrow">Evaluation samples</div><div className="mt-4 grid gap-4 lg:grid-cols-2">{demoIntents.map((intent,i)=><EvaluationCard key={i} agent={demoAgents[0]} intent={intent}/>)}</div></div></>}
