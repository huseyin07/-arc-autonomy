import type { Agent } from "@/types/agent";
import { AddressText, Status } from "@/components/ui";
export function AgentCard({ agent }: { agent: Agent }) { return <article className="panel"><div className="flex justify-between gap-4"><div><h3 className="font-medium">{agent.name}</h3><p className="mt-1 text-xs text-muted">{agent.description}</p></div><Status tone={agent.status==="active"?"ok":"warn"}>{agent.status.replace("_"," ")}</Status></div><div className="mt-7 grid grid-cols-2 gap-5 border-t border-line pt-5"><div><div className="label">Balance</div><strong className="font-mono text-xl">{agent.balance.toFixed(2)} <small className="text-xs text-muted">USDC</small></strong></div><div><div className="label">Daily limit</div><strong className="font-mono text-xl">{agent.dailySpendLimit.toFixed(2)} <small className="text-xs text-muted">USDC</small></strong></div></div><div className="mt-5 text-muted"><AddressText value={agent.walletAddress}/></div></article>; }
