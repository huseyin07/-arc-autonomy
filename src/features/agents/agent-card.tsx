import type { Agent } from "@/types/agent";
import { AddressText, Status } from "@/components/ui";
export function AgentCard({ agent }: { agent: Agent }) { return <article className="panel group overflow-hidden transition duration-200 hover:-translate-y-0.5 hover:border-acid/20 hover:shadow-[0_22px_65px_rgba(0,0,0,.28)]"><div className="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-acid/35 to-transparent opacity-0 transition group-hover:opacity-100"/><div className="flex justify-between gap-4"><div><div className="mb-3 flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-acid/80 shadow-[0_0_12px_rgba(149,245,195,.5)]"/><span className="font-mono text-[9px] uppercase tracking-[.18em] text-muted">Agent identity</span></div><h3 className="text-lg font-medium tracking-tight text-white">{agent.name}</h3><p className="mt-1.5 max-w-xl text-xs leading-5 text-muted">{agent.description}</p></div><Status tone={agent.status==="active"?"ok":"warn"}>{agent.status.replace("_"," ")}</Status></div><div className="mt-7 grid grid-cols-2 gap-4 border-t border-white/[.06] pt-5"><div className="rounded-lg border border-white/[.05] bg-black/15 p-3"><div className="label">Balance</div><strong className="font-mono text-xl font-medium text-white">{agent.balance.toFixed(2)} <small className="text-[10px] font-normal text-muted">USDC</small></strong></div><div className="rounded-lg border border-white/[.05] bg-black/15 p-3"><div className="label">Daily limit</div><strong className="font-mono text-xl font-medium text-white">{agent.dailySpendLimit.toFixed(2)} <small className="text-[10px] font-normal text-muted">USDC</small></strong></div></div><div className="mt-5 flex items-center justify-between gap-3"><span className="font-mono text-[9px] uppercase tracking-[.16em] text-muted">Arc wallet</span><AddressText value={agent.walletAddress}/></div></article>; }
