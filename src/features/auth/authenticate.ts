export interface Signer { address:`0x${string}`; signMessage(message:string):Promise<`0x${string}`> }
async function json(response:Response){const value=await response.json();if(!response.ok)throw new Error(value.error?.message??"Authentication failed.");return value}
export async function authenticateOwner(signer:Signer){const challengeResponse=await json(await fetch("/api/auth/challenge",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({address:signer.address})}));const challenge=challengeResponse.data;const signature=await signer.signMessage(challenge.message);return json(await fetch("/api/auth/verify",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({challengeId:challenge.challengeId,address:signer.address,signature})}))}
